PyYAML>=6.0
requests>=2.28.0
google-api-python-client>=2.70.0
google-auth>=2.20.0
google-auth-httplib2>=0.1.0

# Note: Development tools (linters/formatters/tests) are listed in
# `requirements-dev.txt` to avoid installing them for runtime deployments.
